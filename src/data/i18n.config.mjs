export const defaultUiLocale = "en-US";

export const supportedUiLocales = ["zh-CN", "zh-TW", "ja-JP", "en-US"];

export const supportedRouteLocales = supportedUiLocales.map((locale) => locale.toLowerCase());

export const defaultRouteLocale = defaultUiLocale.toLowerCase();
